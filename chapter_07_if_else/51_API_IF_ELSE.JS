let statusCode = 200;
if (statusCode === 200) {
    console.log("API is working fine")
} else if (statusCode === 404) {
    console.log("API not foound")
} else {
    console.log("No status code match")
}
