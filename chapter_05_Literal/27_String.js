//single quotes
let single == 'Hello, World!';
console.log(single); // Hello, World!
let withDouble = "Hello, World!";
console.log(withDouble); // Hello, World!   


let withDouble = 'She said "Hi!"';
console.log(withDouble); // She said "Hi!"
let c = 'c'; // Character literal (string of length 1)
let c1 = 'cc'; // String literal (length greater than 1)
console.log(c); // c
console.log(c1); // cc  
console.log(typeof c); // "string"
console.log(typeof c1); // "string"
'JavaScript prefers to use single quotes for string literals, but double quotes are also valid.
Using single quotes allows you to include double quotes inside the string without needing to escape them, and vice versa. 
Both single and double quotes create string literals, and JavaScript does not have a separate character type; 
a single character is simply a string of length 1.' 


